---
"mattpocock-skills": minor
---

Add loop-engineering skills for Cursor and other agents: **`setup-agent-loops`** (per-repo verification, stop rules, and scope), **`until-done`** (goal-based loop entry point), and **`agent-loop`** (Plan → Act → Observe → Verify → Stop discipline). Integrates with `/implement`, `/ask-matt`, and `/setup-matt-pocock-skills`. Includes starter loop recipes for build-test-fix, implement-issue, scheduled triage, and PR babysitting.
