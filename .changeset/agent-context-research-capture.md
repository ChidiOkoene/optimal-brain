---
"chidi-optimal-brain": minor
---

Consumer repos now scaffold agent config under `.agent/context/` (legacy `docs/agents/` fallback preserved). Shipped `decision-mapping` ideation skill with maps under `.agent/context/decision-maps/`. Research runs must capture session hubs + finding notes per RESEARCH-CAPTURE-FORMAT. Added `implement` to plugin.json. Updated PLAYBOOK and brain.md for consumer layout.
