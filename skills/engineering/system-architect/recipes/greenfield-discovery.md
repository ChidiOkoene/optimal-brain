# Recipe: Greenfield discovery

**Use when:** new product/system, unclear problem, early architecture framing.

**Requires:** `/setup-optimal-brain` recommended.

```
/system-architect discovery: frame the problem for {{PRODUCT}}. Run context, domain, light NFR, research if vault gaps, then options. Write session hub under .agent/context/architecture-sessions/. Stop when synthesis + next step exist. Do not implement code.
```

## Tips

- If fog of war is large, bootstrap `/decision-mapping` first and link from the session hub.
- Keep roles ≤5 per ENGAGEMENT-TYPES discovery.
