# External Research Lands Best in the Vault

When fresh information is gathered via browser or search tools (e.g. davidondrej research-and-web skills), the highest-leverage move is to immediately persist it as a vault source + synthesis note rather than leaving it in transient chat context.

This learning record captures that pattern after using external tools to surface recent material on agent memory and then synthesizing inside the configured vault.

## Implications

- Future engineering or research sessions that touch memory, RAG, or long-term context will consult the vault first.
- Teach sessions on related topics will see this as prior knowledge via the bridged record and wikilinks.
- Maintenance loops can keep the synthesis current as new sources arrive.

## Sources

- [[Agent Memory Systems Note]] (companion to recently ingested survey + transcript PDFs)
- [[Learning Records Index]]
- `research-from-vault` run inside `/until-done` goal

See also the active teach workspace that originally explored the topic and the local `000x-*.md` record for session-specific detail.
